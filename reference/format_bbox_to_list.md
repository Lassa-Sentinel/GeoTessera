# Convert bbox to named list

Converts various bbox formats to a named list with xmin, ymin, xmax,
ymax.

## Usage

``` r
format_bbox_to_list(bbox)
```

## Arguments

- bbox:

  Numeric vector (4 elements), named vector, list, or sf bbox

## Value

Named list with xmin, ymin, xmax, ymax
