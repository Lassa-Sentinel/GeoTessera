# Discover tiles by format

Returns a named list with tiles organized by format.

## Usage

``` r
discover_formats(directory)
```

## Arguments

- directory:

  Directory to search

## Value

Named list with "npy", "geotiff", and "zarr" elements
