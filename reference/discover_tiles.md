# Discover tiles in a directory

Auto-detects format and finds all tile files.

## Usage

``` r
discover_tiles(directory, format = NULL)
```

## Arguments

- directory:

  Directory to search

- format:

  Specific format to look for (NULL for auto-detect)

## Value

List of Tile objects
