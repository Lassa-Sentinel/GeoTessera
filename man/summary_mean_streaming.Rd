% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize.R
\name{summary_mean_streaming}
\alias{summary_mean_streaming}
\title{Streaming Mean Summary (Memory Efficient)}
\usage{
summary_mean_streaming(
  gt,
  tiles_df,
  year,
  region = NULL,
  sample_rate = 1,
  seed = NULL,
  progress = TRUE
)
}
\arguments{
\item{gt}{GeoTessera object}

\item{tiles_df}{Data frame of tile metadata}

\item{year}{Integer year}

\item{region}{sf object for masking (optional)}

\item{sample_rate}{Fraction of pixels to sample (0-1). Default 1.0 (all pixels).
Use lower values for faster processing of very large regions.}

\item{seed}{Random seed for sampling (if sample_rate < 1)}

\item{progress}{Show progress}
}
\value{
Named list with mean embedding and pixel count
}
\description{
Computes mean embedding using Welford's online algorithm, processing
one tile at a time without storing all embeddings in memory.
Ideal for large regions with many tiles.
}
\examples{
\dontrun{
gt <- geotessera()
tiles_df <- gt$get_tiles(bbox, year = 2024)
result <- summary_mean_streaming(gt, tiles_df, 2024)
}
}
