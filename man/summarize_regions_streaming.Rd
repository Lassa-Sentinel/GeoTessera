% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize.R
\name{summarize_regions_streaming}
\alias{summarize_regions_streaming}
\title{Summarize Multiple Regions with Optimized Tile Scheduling}
\usage{
summarize_regions_streaming(
  gt,
  regions,
  year,
  region_ids = NULL,
  sample_rate = 1,
  mask_to_region = TRUE,
  seed = NULL,
  progress = TRUE
)
}
\arguments{
\item{gt}{GeoTessera object}

\item{regions}{List of sf objects, or a single sf/sfc with multiple features}

\item{year}{Integer year}

\item{region_ids}{Optional character vector of region identifiers.
If NULL, uses row indices or names from the regions.}

\item{sample_rate}{Fraction of pixels to sample per tile (0-1). Default 1.0.}

\item{mask_to_region}{If TRUE, only include pixels inside each region's
polygon. Default TRUE.}

\item{seed}{Random seed for reproducible sampling}

\item{progress}{Show progress. Default TRUE.}
}
\value{
Named list with:
\itemize{
\item summaries: Named list of mean embeddings per region
\item pixel_counts: Named vector of pixel counts per region
\item metadata: Processing statistics
}
}
\description{
Efficiently summarizes embeddings for multiple regions by:
\enumerate{
\item Building a region-to-tile mapping
\item Processing tiles in an order that minimizes redundant downloads
\item Computing streaming statistics for each region
\item Cleaning up tiles as soon as no regions need them
}
}
\details{
This is much more efficient than processing regions independently when
regions share tiles (e.g., adjacent administrative units).
}
\examples{
\dontrun{
library(sf)
gt <- geotessera()

# Load LGAs for a state
lgas <- st_read("nigeria_lgas.shp")
state_lgas <- lgas[lgas$state == "Abia", ]

# Summarize all LGAs efficiently
result <- summarize_regions_streaming(
  gt = gt,
  regions = state_lgas,
  year = 2024,
  region_ids = state_lgas$adminName,
  sample_rate = 0.1
)

# Access results
result$summaries[["Aba North"]]
}
}
