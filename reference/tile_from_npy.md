# Create Tile from NPY files

Create Tile from NPY files

## Usage

``` r
tile_from_npy(embedding_path, base_dir = NULL)
```

## Arguments

- embedding_path:

  Path to embedding .npy file

- base_dir:

  Base directory containing the tile structure

## Value

Tile object
